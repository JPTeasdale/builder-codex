# Frontend Testing

- Run the repo's existing lint, typecheck, unit, and E2E commands when available.
- For UI changes, verify the rendered page with screenshots or browser inspection.
- Check loading, empty, error, and long-content states when the touched surface supports them.
- Prefer tests around user-visible behavior over snapshots that encode incidental markup.
