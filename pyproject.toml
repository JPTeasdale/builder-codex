[project]
name = "builder-codex"
version = "0.1.0"
description = "Source-controlled Codex development workflow, skill, knowledge vault, and orchestrator."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
builder-codex = "orchestrator.builder_codex.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
